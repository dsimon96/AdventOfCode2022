from typing import TextIO

Blizzards = dict[int,]

def part1(inp: TextIO) -> int:
    s = [line.rstrip() for line in inp.readlines()]
    left_blizzards =
    startx = s[0].index(".")
    endx = s[-1].index(".")
